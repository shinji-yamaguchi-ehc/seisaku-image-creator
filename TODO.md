# TODO

## 進行中

（なし）

## 予定 / Backlog

- tailwind.config.js が TypeScript 構文のため oxlint がパースエラーになる（既存問題・設定ファイルの拡張子見直しなど要対応）
- e2e テスト本体の高速化: 固定 `waitForTimeout` の条件待ちへの統一、ピクセルサンプリングの一括化（evaluate 往復削減）

## 完了

- [x] e2e テストの実行時間短縮（並列実行ランナー・個別実行スクリプト追加、AGENTS.md に反復時の使い分けルールを明記）
  - `npm run test:e2e` を2テストファイルの**並列実行**に変更（新設 `e2e/run-all.mjs`。各テストは Vite サーバーをポート0で起動し一時ファイル名も重複しないため競合なし）。フル suite 約7〜8分 → **約2.8分**
  - `test:e2e:resize` / `test:e2e:gradient` を追加し、反復中は関連1ファイルのみ実行可能に
  - AGENTS.md に「実装中の反復は `tsc -b` ＋ 関連1ファイルのみ、フル suite はコミット前の1回だけ」のルールを追記
  - README の E2E セクションに個別実行コマンドと並列実行の説明を追記（グラデ項目数を実態の31に修正）

- [x] develop push を `<repo>/dev` に独立公開する GitHub Pages デプロイへ移行（gh-pages ブランチ方式）
  - `.github/workflows/deploy-pages.yaml`（Actions 版デプロイ）を廃止し、`deploy-pages-main.yaml` / `deploy-pages-develop.yaml` に分割
  - 各ワークフローは gh-pages ブランチ上の自分の担当ディレクトリのみを更新（develop push が main の公開へ影響しない。逆も同様）
  - 同時 push 時は concurrency グループ `pages-publish` で直列実行して競合を防止
  - CI で `vite build --base=/seisaku-image-creator/`（develop は `/dev/` 付き）を指定してアセットパスを解決
- [x] GitHub Actions で main push 時に GitHub Pages（リポジトリ名直下）へ dist を公開するワークフロー追加
  - `.github/workflows/deploy-pages.yaml` を作成（サンプル準拠）
  - SPA ルーティングを Pages 配信でも動かすため `BrowserRouter` → `HashRouter` に変更
  - `vite.config.ts` に `base: "./"` を追加（リポジトリ名非依存のアセットパス）

## 予定 / Backlog

- tailwind.config.js が TypeScript 構文のため oxlint がパースエラーになる（既存問題・設定ファイルの拡張子見直しなど要対応）

## 完了

- [x] グラデーション機能のプリセットを削除（プリセットチップの UI・型定義・e2e を廃止）
  - `GradientControls` からプリセットセクションを削除
  - `gradient.ts` から `GradientPreset` 型・`GRADIENT_PRESETS` 定数を削除
  - e2e のプリセットクリック箇所をすべて-sided ボタン・色入力・不透明度スライダーへの直接操作に置き換え（31項目すべて PASS）
- [x] コピーライト表示を追加: `Copyright © エンパワーヘルスケア株式会社 All Rights Reserved.`
  - App.tsx のフッターに全ページ共通で表示。各ページの `min-h-screen` を削除して flex レイアウトに統合
  - レイアウトページ 73項目 + グラデーションページ 32項目 = 105項目すべて PASS
- [x] グラデーションページの既定キャンバス高さを 345px に変更（PC/SP 共通）
  - `GradientTool` の既定 config を更新し、e2e の既定値・フィット位置・ズームアンカー連鎖・出力サイズ（1024x385）の期待値を再計算して修正（32項目すべて PASS）
- [x] グラデーションの既定スタイルを変更: 色 #ffffff・開始の不透明度 100%・終了 0%（右端から中央へ白→透明のフェード）
  - `DEFAULT_GRADIENT_STYLE` の color/startAlpha を更新し、既定スタイル検証の e2e 期待値も白フェード向けに修正（32項目すべて PASS）
- [x] グラデーションの向きに「左右（両端から内側）」を追加し、カスタム項目を向き間で共有
  - `GradientStyle.side` に `"both"` を追加。描画は右フェードと左フェードを順に重ねて合成（`createSideGradient` に分離）
  - 向き切替時の位置リセットを廃止。開始位置・終了位置・色・不透明度は共有され、内部で side 側のストップ割り当て（枠端からの距離）だけが反転する
  - プリセットを刷新（右白フェード／左黒フェード／右ネイビー／左右黒ワイド）
  - e2e を更新: 向き切替でのミラー反映、左右フェードの両端白・中央暗のピクセル検証などを追加し 32 項目すべて PASS
- [x] 縮小時のはみ出し移動を許可（案A）: 縮小した画像を意図的にフレーム外へ一部はみ出させ、不要な端を見せない配置ができるようにした
  - `transform.ts` の `axisFocus` 縮小ブランチを変更: 「画像全体が枠内に完全収まる」制約 → 「画像の一部が見えている限り自由（はみ出し可）」に緩和。拡大時（トリミング）は従来どおり枠からはみ出さない範囲
  - 余白＋はみ出しを組み合わせた構図が両ツールで可能に。はみ出した部分は出力に描画されない
  - UI テスト追加: レイアウト（5%縮小→左にはみ出し配置、余白は白・はみ出し部は非描画のピクセル検証）、グラデ（38%状態での左はみ出し、余白にはフェードが乗ることのピクセル検証）
  - e2e: resize 73 項目／gradient 31 項目すべて PASS
- [x] ズーム下限を cover フィットから解放し、余白が出る縮小を可能に（両ツール共通）
  - `transform.ts`: 下限を定数 `ZOOM_MIN = 0.05`（5%）に変更。`minZoom()` は `coverZoom()` に改名し「フィット操作・警告判定」専用に
  - 縮小時も画像が完全に枠外へ出ないよう focus をクランプ（axisFocus を拡大時と統一形式で一般化）
  - 余白警告の意味を変更: 現在のズーム状態ではなく「元画像自体が枠より小さい（100% でも余白が出る）」場合のみ表示（`hasGap(t,…)` → `smallerThanFrame(imgW,imgH,…)`）
  - UI テストを追加: 「-」連打で下限 5% まで縮小＆下限クランプ、フィットからホイールアウトで cover 以下へ縮小（グラデ側）など
  - e2e: resize 71 項目／gradient 29 項目すべて PASS
- [x] グラデーションを左右フェード専用に簡素化（左のみ／右のみを選択、開始位置・終了位置を UI で指定可能に）
  - `GradientStyle` を `{ side: "left"|"right", startPos, endPos, color, startAlpha, endAlpha }` に変更。ラジアル・自由角度は廃止
  - 描画は水平方向の `createLinearGradient` のみ。範囲外は各端の色が延長されるため、「右端から中央へ白→透明」のような指定が可能
  - UI: 向き切替ボタン（切替時は位置を既定値へリセット）／開始位置・終了位置のスライダー＋数値入力／フェード色（カラーピッカー＋hex 直入力）／開始・終了の不透明度スライダー
  - プリセットを左右系に刷新（右白フェード／左黒フェード／右ネイビー／右黒ワイド）
  - 拡大禁止・縮小OK ルール（ズーム上限 100%、両ツール共通＝`transform.ts` 共用）を README に明記
  - 実装中に発見・修正した不具合:
    - `hexToRgb` 用の正規表現 `HEX_RE` にキャプチャグループがなく、`m[1]` が常に undefined となり色が必ず黒になるバグ（グループ付き `/^#([0-9a-fA-F]{6})$/` に修正）
    - e2e 側: フィット状態からのズームアウトは下限クランプで不変になる点、サイド切替時に位置が既定へ戻る点、終了位置より内側は終了色が延長される点について期待値を実装仕様に合わせ修正
  - `e2e/e2e-gradient.mjs` を左右フェード仕様向けに更新し 26 項目すべて PASS
- [x] グラデーション作成機能の追加（react-router-dom 導入、`/gradient` ページ新設。計画は function-plan.md のとおり。計画書は実装完了に伴い削除）
  - ルーティング: `react-router-dom@^7.18` 導入、`src/main.tsx` に `BrowserRouter`、`App.tsx` をナビシェル化（`/` = レイアウト作成、`/gradient` = グラデーション作成）。既存機能一式は `src/features/layout/LayoutTool.tsx` へ移動し既存 e2e は無修正で回帰 PASS
  - 状態モデル: `src/lib/gradient.ts`（GradientCanvasConfig / GradientStyle / 正規化 / プリセット4種 / CSS準拠の角度計算）
  - 描画: `canvas-renderer.ts` に `drawGradientLayout`（白背景 → 画像 → 全面オーバーレイ）を追加。プレビューとエクスポートで同一関数を使用し WYSIWYG を保証
  - UI: `GradientCanvas`（単一フレーム版キャンバス：ドラッグ／カーソルアンカー付きホイールズーム／ダブルクリックでフィット⇔100%／キーボード操作／下端ハンドル＝高さ・左右ハンドル＝幅のドラッグ変更／%バッジの数値入力パネル）、`GradientControls`（プリセットチップ・リニア⇔ラジアル・角度・色2つ＋透明度）、`GradientExportDialog`（`gradient_pc.png` / `gradient_sp.png`）
  - PC/SP はキャンバスサイズ・transform・グラデ設定とも完全独立。差し替え時は両モードの transform をリセット
  - 初回ショートカットオーバーレイを App シェルに移動（どちらのページでも初回アクセス時に表示）
  - `e2e/e2e-gradient.mjs` を新設（25項目）しすべて PASS。実装中に発見・修正したテスト側の問題:
    - プレビューは出力と 1:1 表示のため、画像デコード・再描画の反映待ちを固定 sleep から条件待ち（waitGradient/waitPx）に変更
    - フィット状態（zoom=下限）からのズームアウトは下限クランプで不変になるため、ズームインでの検証に変更（カーソルアンカー込みの期待値も再計算）
    - プリセット「上白フェード」の角度定義誤り（CSS 準拠では 0° の開始ストップは下端になる）を 180° に修正
    - PC/SP 独立性検証で「PC復帰後のスタイル」の前提誤りを修正（D セクション最後に選んだスタイルが保持されるのが正）
- [x] スロット直接アップロード・差し替え・削除の実装（ui-plan2 の計画どおり。計画書は実装完了に伴い削除）
  - 空スロットをクリック／ドロップで直接アップロード可能に（共有の hidden file input を1つ使い回し、対象スロット番号は ref で管理）
  - 画像ありスロットにホバーすると右上へ「変更」「削除」ボタンを表示（`data-controls` 付きでパン・ホイールと競合しない。Tab 順に入れないよう `tabIndex={-1}`）
  - ドロップは画像ありスロットにも常時受け付け（上書き差し替え）。削除で空きスロットに戻る
  - 画像読み込み処理を `src/lib/image-loader.ts`（`fileToImageSlot`）に共通化し、下部アップロード枠の input に testid を付与
  - 差し替え・削除時に古い `objectUrl` を `URL.revokeObjectURL` で解放
  - 実装中に発見・修正した不具合:
    - 幅ドラッグ中に毎回 `getBoundingClientRect()` を取り直すと、キャンバスが `mx-auto` 中央寄せのため再センタリング分が座標に乗り、ドラッグ量が累積するバグ（低速環境で顕在化）。ドラッグ開始時の rect を DragState に固定して解消
    - e2e の古い期待値（フィット時 top=-162 → 正しくは -158、矢印ナッジ方向）と、幅変更後のハンドル座標を使い回していたテスト側の問題を修正
  - e2e にスロット直接操作の検証（削除・クリック追加・変更ボタン・ドロップ上書き・空きスロットへのドロップ）を追加し 70 項目すべて PASS
- [x] 四隅ハンドルを廃止し、左右エッジハンドル（キャンバス幅のみ変更）に置き換え
  - 幅ドラッグ中に換算係数 k が最新 config から再読み込みされ、中間イベントで過剰カウントされる不具合を発見・修正（換算係数をドラッグ開始時の値で固定）
  - e2e 65 項目すべて PASS
- [x] 境界線ドラッグの挙動を変更：1行目の高さのみを変更するようにし、2行目はキャンバス下端のドラッグで独立に変更できるようにした（従来は合計高さを保存して配分を入れ替えていた）
  - 行間の境界線＝1行目の下端、キャンバス下端に新設したハンドル＝2行目の下端として、それぞれ相手側の行・余白を変えずに高さのみ変更
  - キャンバス全体の高さはドラッグに応じて自動的に伸縮
  - e2e に行の独立性検証を追加し 65 項目すべて PASS。あわせてアップロード後の固定待機による競合を条件待ちに修正
- [x] 1行目・2行目の高さを独立設定できるように変更。キャンバス高さの直接入力を廃止（行高＋余白から自動導出）、行間の余白（px）を追加
  - `LayoutConfig` を `{ canvasWidth, row1Height, row2Height, rowGap }` に変更し、1行目アスペクト比の上限と行高比率スライダーを廃止
  - 行間の境界線ドラッグは「全体の高さを保ったまま配分を変更」する挙動に変更
  - 四隅ハンドルは幅の倍率を行高・余白にも適用する方式に変更。実装中に発見・修正した不具合: ドラッグ中の最新 config を基準にして中間ポインタイベントで倍率が累積するバグ（開始時 config を基準にするよう修正）
  - `e2e/e2e-resize.mjs` に行間余白テスト等を追加し 62 項目すべて PASS
- [x] ui-plan.md の実装：Transform 正規化モデル移行、canvas プレビュー統一＋SVG/HTML オーバーレイ、行境界ドラッグと四隅ハンドルによるフレームリサイズ、キーボード操作・数値入力・ヘルプ UI
  - `Transform` を `{ zoom, focusX, focusY }` の正規化座標モデルへ移行し、幾何計算はキャンバス座標（エクスポート空間）基準に統一。PC/SP のプレビュー幅差でも WYSIWYG が崩れない
  - プレビューを `<canvas>` 1枚（devicePixelRatio 対応）に統一し、エクスポートと同一の `drawLayout` を流用
  - マウス操作（ドラッグ／カーソルアンカー付きホイールズーム／ダブルクリックでフィット⇔100%）・行間境界線ドラッグ・四隅ハンドル（アスペクト比固定）を実装
  - キーボード操作（Tab 移動、矢印ナッジ、+/-、Ctrl+0/Ctrl+1）、スロット調整パネル（ズーム%・オフセットpx の数値入力）、初回ショートカットオーバーレイ（localStorage 管理）を追加
  - 実装中に発見・修正した不具合: ドラッグ移動の基準を「現在の transform」にしていたため、中間ポインタイベントで移動量が累積するバグ（ドラッグ開始時の transform を基準にするよう修正）
  - `e2e/e2e-resize.mjs` を新UI向けに再作成し 57 項目すべて PASS
- [x] Playwrightで画像のリサイズ（スケール変更・ドラッグ移動）とエクスポートの動作を確認する
  - 発見・修正した不具合:
    - レイアウト幾何計算が LayoutCanvas / App / canvas-render の3箇所で重複し、パディング(8px)の扱いが不一致 → 極端なアスペクト比の画像で出力の1行目下端に白帯16pxが入るバグ。`src/lib/layout.ts` に共通計算を切り出して解消
    - `drawImage(File)` が環境により TypeError になる非標準実装 → デコード済み `HTMLImageElement` を `ImageSlot.element` として保持する方式に変更
- [x] 画像を差し替えたときに前画像の transform が引き継がれる問題 → アップロード時に全モードの transform をリセットして解消
- [x] プレビューコンテナの実サイズと prop 値の border 分の差（最大4px）→ img ベースのプレビュー廃止により構造的に解消

(End of file)
