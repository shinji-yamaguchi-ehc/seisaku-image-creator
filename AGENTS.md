# AGENTS.md

このリポジトリ（プロジェクト）で作業を行うエージェント（コーディングエージェント）向けの指示です。このファイルのスコープ内のファイルを変更する際は、以下のルールに従ってください。

## Git ブランチ戦略（厳守）

**main ブランチへの直接コミット・マージは禁止です。** 全ての変更は GitHub Flow に従ってください。

### 作業手順

1. **ブランチ作成**: `git checkout -b feature/<機能名>` または `fix/<修正名>`
2. **実装・コミット**: 実装完了後、コンパイル・e2e テスト通過を確認してからコミット
3. **プッシュ**: `git push origin <ブランチ名>`
4. **PR 作成**: GitHub 上で main ブランチへの Pull Request を作成

### コミットルール

- コミットメッセージは [Conventional Commits](https://www.conventionalcommits.org/) に従う
  - `feat: 新機能追加`
  - `fix: バグ修正`
  - `refactor: リファクタリング`
  - `chore: ドキュメント・CI 等`
- コミット前に以下を確認すること:
  - `npx tsc -b`（型チェック）
  - `npm run test:e2e`（e2e テスト全項目パス）
- 1 コミット = 1 機能・1 修正（まとめコミットは避ける）
- メッセージは日本語でも英語でも可（プロジェクトに統一があれば従う）

### ブランチ命名規則

| 種別 | 命名例 |
|---|---|
| 機能追加 | `feature/gradients-preset` |
| バグ修正 | `fix/drag-cumulative-bug` |
| ドキュメント・CI | `chore/add-copyright` |

## 進捗管理（TODO.md）

- 作業を始める前に、対象タスクを [TODO.md](TODO.md) の「進行中」へ移動させてください。
- タスクを完了したら「進行中」から「完了」へ移動し、チェックボックスを `[x]` に更新してください。
- 新たに判明した作業は [TODO.md](TODO.md) の「予定 / Backlog」に追記してください。
- 進捗の更新は、その作業を行ったターン内で行ってください（後回しにしない）。

## README.md の並行更新

- ユーザーに見える変化（機能追加、構成変更、仕様変更、セットアップ手順の変更など）があった場合、その都度 [README.md](README.md) を並行して更新してください。
- リポジトリに [README.md](README.md) が存在しない場合は、作業の開始時に新規で作成してください（プロジェクト名・概要・セットアップ手順などを記載した日本語のテンプレートを用意する）。
- 更新は最小限で本質的なものに留め、無関係な再構成や過度な装飾は行わないでください。
  - README に追加した情報は、[TODO.md](TODO.md) の関連タスクにも反映してください。

## 文書作成のルール

- README、TODO、AGENTS などの文書は日本語で記述してください。
- コーディングに関する具体的な規約は、このプロジェクトの技術スタックが決まった時点で追加します。

## 重要

- このファイルの指示は、より具体的なルール（ユーザー指示など）が優先されます。
- 変更は常にリポジトリの状態を壊さないよう、慎重に行ってください。
